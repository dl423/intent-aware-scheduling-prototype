# The mock experiment requires no credentials.
INTENT_SCHEDULER_PROVIDER=mock
INTENT_SCHEDULER_DB=scheduler.db
INTENT_SCHEDULER_CAPACITY=6

# Optional real-provider service only. Never commit actual credentials.
OPENAI_API_KEY=
